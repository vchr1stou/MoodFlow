specVersion: "v1"
serviceId: "moodflow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "moodflow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
